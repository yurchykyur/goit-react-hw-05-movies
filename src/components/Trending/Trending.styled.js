import styled from 'styled-components';

const Wrapper = styled.div`
  padding-left: 40px;
`;

const Title = styled.h2`
  font-size: 24px;
  margin: 0 0 20px 0;
  padding: 0;
`;

const Item = styled.li`
  font-size: 18px;
  font-weight: 600;
  margin: 0 0 10px;
`;

export { Title, Wrapper, Item };
